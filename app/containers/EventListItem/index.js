/* import { connect } from 'react-redux';
import { createStructuredSelector } from 'reselect';
import EventListItem from './EventListItem';

export default connect(
  createStructuredSelector({
    currentUser: ''
  })
)(EventListItem);
 */
export { default } from './EventListItem';
